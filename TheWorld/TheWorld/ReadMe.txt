When minifying the js files:

Go to command prompt and execute:

	npm install gulp
	npm install -g gulp-cli

So that gulp could be executed from the command line.

Then execute:
	
	gulp minify

It will execute the gulp task found in gulpfile.js